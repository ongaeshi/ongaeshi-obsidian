---
tags:
  - categories
aliases:
  - permanent notes
---

```base
formulas:
  other_links: file.links.filter(value.asFile().name != "永続メモ")
views:
  - type: table
    name: すべて
    filters:
      and:
        - file.name != "永続メモテンプレート"
        - or:
            - categories.contains(link("永続メモ"))
            - file.folder == "07_Thinking/02_永続メモ"
    order:
      - file.name
      - created
      - formula.other_links
    sort:
      - property: created
        direction: DESC
      - property: file.mtime
        direction: DESC
      - property: formula.other_links
        direction: ASC
      - property: previous
        direction: ASC
    columnSize:
      file.name: 339
      note.created: 128
  - type: table
    name: 未接続
    filters:
      and:
        - file.hasProperty("finished")
        - finished == null
        - categories.contains(link("永続メモ"))
        - file.name != "永続メモテンプレート"
    order:
      - file.name
      - created
    sort:
      - property: file.mtime
        direction: DESC
      - property: file.name
        direction: DESC
    columnSize:
      file.name: 480
  - type: table
    name: 全プロパティ
    filters:
      and:
        - file.name != "永続メモテンプレート"
        - or:
            - categories.contains(link("永続メモ"))
            - file.folder == "07_Thinking/02_永続メモ"
    order:
      - file.name
      - created
      - finished
      - formula.other_links
    sort:
      - property: created
        direction: DESC
      - property: file.name
        direction: DESC
      - property: previous
        direction: DESC
      - property: finished
        direction: DESC
      - property: file.mtime
        direction: DESC
    columnSize:
      file.name: 300
  - type: list
    name: タイトルのみ
    filters:
      and:
        - file.name != "永続メモテンプレート"
        - or:
            - categories.contains(link("永続メモ"))
            - file.folder == "07_Thinking/02_永続メモ"
    order:
      - file.name
    sort:
      - property: file.mtime
        direction: DESC

```
