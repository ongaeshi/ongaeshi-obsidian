---
tags:
  - categories
aliases:
  - reading
---

```base
views:
  - type: table
    name: 読んでる
    filters:
      and:
        - file.name != "読んでるテンプレート"
        - or:
            - categories.contains(link("読んでる"))
        - finished.isEmpty()
    order:
      - file.name
      - created
      - finished
    sort:
      - property: file.tags
        direction: DESC
      - property: created
        direction: DESC
      - property: finished
        direction: ASC
    columnSize:
      file.name: 339
      note.created: 166
  - type: table
    name: すべて
    filters:
      and:
        - file.name != "読んでるテンプレート"
        - or:
            - categories.contains(link("読んでる"))
    order:
      - file.name
      - created
      - finished
    sort:
      - property: finished
        direction: DESC
      - property: created
        direction: DESC
      - property: file.tags
        direction: DESC
    columnSize:
      file.name: 339
      note.created: 166

```
